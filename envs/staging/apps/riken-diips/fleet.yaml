# WordPressサイトではない自作アプリ(riken-diips)。
# DBを持たないステートレスなフロントエンドのため、sites/ とは分けてapps/配下に置く
# (keepResources等WordPressサイト固有の注意点はここには適用されない)。
# 詳細は docs/manual-apps.md 参照。
defaultNamespace: riken-diips
targetNamespace: riken-diips

keepResources: true

helm:
  releaseName: riken-diips
