defaultNamespace: longhorn-system
targetNamespace: longhorn-system

# バンドルがGitから消えたりGitRepoが付け替えられたりしても、Fleetにリソースを
# 削除させない(ストレージ層のアンインストール=全ボリューム喪失につながるため)。
# マルチ環境化でバンドルの置き場所を移設する際の安全装置でもある。
# docs/manual-multi-env.md 参照。
keepResources: true

helm:
  # 旧GitRepo(base-infra)時代のHelmリリースをそのまま引き継ぐための名前。
  # これが無いとFleetが別名の新リリースを作ろうとして既存リソースと衝突する。
  # (staging/productionは新規クラスタのため素直な名前 longhorn を使っている)
  releaseName: base-infra-longhorn
  chart: longhorn
  repo: https://charts.rancher.io
  version: 109.3.1+up1.11.2
  values:
    defaultSettings:
      # クラスタ外NFSへのバックアップ先(環境ごとに別ディレクトリ)。
      # 定期実行は ../longhorn-jobs/ のRecurringJobで管理する。
      backupTarget: nfs://192.168.1.1:/data/nfs/longhorn/dev
      # スケジュール可能枠 = (ディスク容量 - 予約) × この比率。
      # 15サイト一斉追加(#55)で既定の100%では枠が尽きたため150%へ。
      # wp-contentはthin-provisioningで実使用が数%と低く、実容量の逼迫は
      # 監視(Longhorn容量アラート)で検知する。恒久策はノード専用ディスク増設
      # (docs/roadmap.md 項目3、本番量産の計画時に判断)。
      storageOverProvisioningPercentage: "150"
