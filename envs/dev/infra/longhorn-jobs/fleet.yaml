defaultNamespace: longhorn-system

# RecurringJobは設定のみ(データ実体はNFS側)だが、infraバンドルの方針に合わせて
# バンドル削除時もリソースを残す。
keepResources: true

helm:
  releaseName: longhorn-jobs
  # BackupTarget `default` はLonghornが自動作成する既存CRのため、
  # 所有権を引き取って上書き管理する。
  takeOwnership: true
